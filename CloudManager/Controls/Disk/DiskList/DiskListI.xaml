<UserControl x:Class="CloudManager.DiskList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CloudManager"
             mc:Ignorable="d" >
    <Grid>
        <Grid Background="Transparent" DataContext="{x:Static local:DisklistDesignModel.Instance}">

            <ListBox ScrollViewer.VerticalScrollBarVisibility="Hidden" VirtualizingPanel.ScrollUnit="Pixel" 
            HorizontalContentAlignment="Stretch"
            Background="Transparent"
            BorderThickness="0"
            ItemContainerStyle="{StaticResource DriveListContainerStyle}"
            ItemsSource="{Binding Items}" >
                
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
